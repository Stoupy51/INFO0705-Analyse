#vider la mémoire
rm(list=ls())
#chargement des données "AUTOS2005subset.txt"
autos <- read.table(file.choose(), row.names=1, header=T,
stringsAsFactors = TRUE, sep="\t")
View(autos)
print(summary(autos))
# Chargement des données "data_analysis.csv"
autos <- read.csv(file.choose(), row.names=1, header=TRUE, stringsAsFactors = TRUE)
print(summary(autos))
str(autos)
View(autos)
#vider la mémoire
rm(list=ls())
# Chargement des données "data_analysis.csv"
autos <- read.csv(file.choose(), row.names=1, header=TRUE, stringsAsFactors = TRUE)
print(summary(autos))
str(autos)
View(autos)
View(autos)
#vider la mémoire
rm(list=ls())
# Chargement des données "data_analysis.csv"
players <- read.csv(file.choose(), row.names=1, header=TRUE, stringsAsFactors = TRUE)
print(summary(players))
str(players)
#fonction pour centrage-réduction
CR <- function(x){
n <- length(x)
m <- mean(x)
v <- (n-1)/n*var(x)
return((x-m)/sqrt(v))
}
#appliquer la fonction sur les variables continues 1:5
players.cont <- data.frame(lapply(subset(players,select=1:5),CR))
